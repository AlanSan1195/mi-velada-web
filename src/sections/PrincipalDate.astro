<section
  class="mt-32 mx-auto flex flex-col justify-center items-center text-center text-secundary"
>
  <h2 class=" animate-fade-in animate-delay-500 uppercase text-5xl font-semibold">
    4 de marzo <span aria-hidden class="hidden md:inline mx-1">.</span><br
      aria-hidden
      class="block md:hidden"
    />
    <span>
      <script is:inline>
        const END_DATE = 17095752000000;
        const d = new Date(END_DATE);
        const date = d.toLocaleTimeString("es", {
          hour: "2-digit",
          minute: "2-digit",
        });

        //recuperar zona horaria
        const tzOffset = d.getTimezoneOffset() / 60;
        const diff = tzOffset * -1;
        const gmt = diff > 0 ? `GMT+${diff}` : `GMT-${Math.abs(diff)}`;

        const TZ_DICTIONARY = {
          "GMT+1": "CET",
        };

        const tz = TZ_DICTIONARY[gmt] ?? gmt;

        const selfScript = document.currentScript;
        selfScript.parentNode.innerHTML = `${date}H  ${tz}`;
      </script>
    </span>
  </h2>
  <h3 class="uppercase text-2xl font-medium flex flex-col max-w-sm mt-6">
    <span>Evento de Presentación</span>
    <span>Teatro Victoria (Barcelona)</span>
  </h3>
  <a
    class={`border-secundary border-2 font-medium text-lg uppercase mt-8 px-4 py-1 hover:scale-125 hover:bg-slate-700 transition inline-block 
    `}
    href="https://www.teatrevictoria.com/es/cartelera/c/190_la-velada-del-ano-4.html"
    target="_blank"
    rel="noopener noreferrer"
  >
    Entradas agotadas
  </a>
</section>
 